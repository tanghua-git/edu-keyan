# edu-keyan 技能包 · 达尔文优化报告（Phase 3）

> 工具：darwin-skill 2.x（9 维 rubric，总分 100）
> 日期：2026-09-18/19　范围：edu-keyan 6 技能
> 方法论：git 棘轮 + paired 多数决（N=3 独立 judge）+ HL-4 见好就收 + 逐技能人审 CHECKPOINT
> 实测：基线 dim8 用 6 个独立 subagent 做「带技能 vs 无技能」对比（full_test）；优化轮用 paired 盲评

## 一、总览

| 指标 | 值 |
|:--|:--|
| 优化技能数 | **6** |
| 总优化轮数 | **18**（每技能 3 轮，全部跑满后触发 HL-4 收敛） |
| keep / revert | **18 / 0（100% 改进接受率）** |
| paired 盲评次数 | 18 轮 × 3 judge = **54 次独立判断** |
| 人审检查点 | 6 次（每技能 1 次，全部确认通过） |
| 改动性质 | 全部为**纯增量**（0 删除），合计 +292 行 |

## 二、分数变化

| Skill | Before | After | Δ |
|:--|:--:|:--:|:--:|
| edu-keyan-shishi | 78.7 | **88.2** | +9.5 |
| edu-keyan | 80.4 | **88.2** | +7.8 |
| edu-keyan-xuanti | 81.0 | **88.2** | +7.2 |
| edu-keyan-chengguo | 81.0 | **88.2** | +7.2 |
| edu-keyan-fangan | 81.4 | **88.6** | +7.2 |
| edu-keyan-fangfa | 82.8 | **88.2** | +5.4 |
| **平均** | **80.9** | **88.3** | **+7.4** |

## 三、共性改进（6 个技能统一注入的四个章节）

1. **失败模式与回退（Failure Modes）** —— 每个技能 6–8 条显式「If→Then」分支表（F 编号）
   - 覆盖：信息不全、条件不足、reference 读取失败、越界请求、热点过期、数据编造等
   - 对应 rubric dim3（权重 12）：全系从 5–6 分提到 8 分
2. **关键检查点（🔴 Checkpoints）** —— 每个技能 3–4 个显性标记门（CP 编号）
   - 覆盖：节点/文体/口径确认、数据真实性门、勾稽自检门、越界终止
   - 对应 rubric dim4（权重 6）：全系从 3 分提到 8 分（此前全系为 0 显性标记）
3. **反例与黑名单（Anti-patterns）** —— 每个技能独立成章的 ❌ 负向清单（10–12 条，分 3 类）
   - 对应 rubric dim9（权重 6）：从 7–9 分提到 9 分
4. **输出规范（Output Contract）** —— 每个技能 6 条输出纪律（先判断后展开、长度上限、工具落地等）
   - 对应 rubric dim8（权重 23）与可执行性 dim5

## 四、棘轮与版本控制

- 每个技能目录独立 git 仓库，baseline 1 commit + 优化 3 commit + 日志 commit
- 全程 **0 次 revert**：18 轮 paired 盲评无一次多数判 worse
- 每技能最后 2 轮（R2 反例黑名单、R3 输出规范）judge 稳定判 **slight better**，触发 darwin **HL-4「连续 2 轮 margin≤slight → break」**，自动收敛，未做无谓的第 4 轮

## 五、结论

- **两个系统性短板（dim3 失败模式 + dim4 检查点）已被彻底补齐**，是本次 +7.4 分的主要来源（R1 单轮即贡献 +5.4~+6.6）。
- 后续三轮（dim9/dim8 强化）边际收益递减但方向一致，全部 keep。
- 全系改动为纯增量，**无内容丢失、无退化**，安全性高。
- 优化后 6 技能得分高度收敛于 88 分档，说明技能包质量已趋一致。

---
*各技能详细日志见 `skills/<skill>/results.tsv`；原始基线报告见 `edu-keyan-darwin-baseline-20260918.md`。*
